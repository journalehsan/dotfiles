# Ignore nested .git directories (but files are already tracked)
# This prevents accidentally creating nested git repos in the future
**/.git/
**/.git

# Common temporary files
*~
.DS_Store
*.swp
*.swo
.temp/
.tmp/